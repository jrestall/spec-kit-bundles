# API Server Stub

This repo represents the backend order API.

It has its own local OpenSpec root for implementation-specific backend design, while referencing the shared `team-planning` store for cross-repo product requirements.
