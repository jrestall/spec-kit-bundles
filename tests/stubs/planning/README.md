# Planning Store

This repo is a standalone OpenSpec store registered locally as `team-planning`.

It owns cross-repo planning artifacts. Code repos either point to it as their default planning home or reference it as read-only context.
