# Web App Stub

This repo represents the frontend checkout experience.

OpenSpec planning is fully externalized here. The only OpenSpec file is `openspec/config.yaml`, which points commands at the `team-planning` store.
